---
import { redirect } from "astro:middleware";
// Redirigir al idioma por defecto
export async function middleware({ redirect }) {
  return redirect('/es'); // Cambia a tu idioma predeterminado
}
---