/* import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    host: '127.0.0.1',
    port: 4321,
    strictPort: true,
    allowedHosts: ['*'], // Permitir cualquier host
    cors: true, // Habilitar CORS
    headers: {
      'Access-Control-Allow-Origin': '*', // Permitir solicitudes externas
    },
  },
}); */

